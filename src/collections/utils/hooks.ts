// export const generateSlug = ({ data, operation }) => {
//   if ((operation === "create" || operation === "update") && data.title && !data.slug) {
//     data.slug = data.title
//       .toLowerCase()
//       .replace(/[^a-z0-9]+/g, "-")
//       .replace(/^-|-$/g, "");
//   }
//   return data;
// };
